# Spec 010 — таб правил: convergence (flow-track)

## Жалоба
«Открываю Traffic rules — нихуя не изменилось»: буквы-бейджи бред,
непонятно что активно.

## Корни (два)
1. Живой процесс (старт 03:22) был СТАРШЕ кода таба (переписан 03:52):
   пользователь видел старый UI физически. Доказано (ps vs mtime).
   Вывод: деплой без рестарта = невидимый фикс. Рестарт сделан (03:59).
2. Заметность: буквы вместо иконок; активный — только рамка+галочка.

## Что теперь (convergence с параллельной переработкой 03:52)
- Карточки: эмодзи, точка verified + тултип, кнопка «>» с диалогом
  (census + «проверено вживую / ещё не проверялось» + «· АКТИВЕН»).
- Плюс моё: настоящие иконки v2RayTun (8 шт, закэшированы на хосте;
  fallback эмодзи→буква), тест диалога (census+АКТИВЕН+бейдж).
- Свой 3-tuple в render, сломавший рантайм чужого кода, откачен мгновенно.

## Verify
31/31 pytest → vision-пруф рендера (иконки, без налезаний) → деплой →
рестарт 03:59 → CHANGES + push.
