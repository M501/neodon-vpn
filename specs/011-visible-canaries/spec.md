# Spec 011 — видно что работает: канарейки в диалоге (flow-track)

## Недоверие владельца
«UI перенесён, а rules не имплементированы» + мелкий флаг в чёрном квадрате.

## Факты
- Матрица 11 пресетов × 8 доменов (`matrix_canary.py`, без свитчей и трафика):
  все 88 ячеек совпадают с замыслом (RU→direct, остальное→proxy/direct по
  флагу). Нарушений паритета НОЛЬ.
- v2RayTun шлёт yandex через category-ru→direct — мы так же (suffix+keyword).
- Иконка ru-bez-vpn.jpg (флаг на чёрном поле) удалена → эмодзи 🇷🇺.

## Видимость (диалог «>»)
- `CANARIES`: 2–3 (домен, ожидание) на пресет, закодированы из матрицы.
- `route_lookup()`: матчинг по ПРИМЕНЁННОМУ config.json (mmap-кэш по mtime).
- Активный пресет: `ya.ru → напрямую ✓` живьём; иначе «…(когда применишь)».
- Несовпадение показало бы `✗ сейчас …` — молчать не умеет.

## Verify
33/33 pytest (lookup + canaries-dialog) → деплой → рестарт → CHANGES + push.
