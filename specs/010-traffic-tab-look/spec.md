# Spec 010 — таб Traffic rules как у v2RayTun (flow-track)

## Жалоба
Таб не менялся на вид; буквы в кружках — бред; активный неясен; не как в v2RayTun.

## Эталон (снят вживую с Windows v2RayTun)
Тёмная карточка: Connection time, Traffic rules и Routing — отдельные строки;
страница пресетов: тумблер «Use preset», инфо-карточка community rules,
строки [иконка] Name [● verified] … [ⓘ], активный без крика.

## Редизайн (наш таб)
- Строки как у эталона: эмодзи-иконки (были в данных, игнорились),
  синяя точка verified, `>` в детали (ⓘ нет в шрифтах — рисовался «0»),
  описания + «остальное — …».
- Активный: синяя рамка карточки + зелёная галочка (тест objectName).
- Тумблер «Use preset»: выкл → Default (запоминает последний), вкл → назад.
- Диалог `>` : census правил, verified-статус, «АКТИВЕН», кнопка Применить.
- Снесены: буквы-бейджи, текстовые строки verified, мёртвый preset_desc,
  QButtonGroup-обвязка.

## Verify
29/29 pytest (toggle-routes, highlight, summary, render) + vision-пруф
рендера + деплой + рестарт → CHANGES + push.
