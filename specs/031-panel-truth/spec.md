# Spec 031 — Честный показ состояния в обеих UI (flow-track)

## Факты с хоста (не предположения)
- backend VPN не останавливался при закрытии панели: vpn_down=0 за день,
  NRestarts=0, exit-IP и TUN-маршрут живы, трафик идёт.
- Панель при ремаунте рисовала Off до первого опроса → «вырублен».
- В трее после возврата в десктоп тишина, потому что мёртв сам процесс
  GUI (сессия прибила, автостарта не было). tray-state.json протухший.
- Таймер обнулялся, т.к. _epoch жил в процессе; active_epoch() был, но
  никем не вызывался.

## Фиксы
- Панель: Syncing… до первого опроса; 3 вызова одним рейсом; убит дубль
  setOn вне quiet-окна (мёртвый код, оставленный в 030).
- Десктоп: seed_epoch() от старта юнита в set_state и _status_loaded;
  тест test_epoch_seeds_from_service_start (3600с ±10).
- Инсталлер: gui-autostart (индикатор, без power); verify warn-чек;
  qa-static + qa-sandbox проверки.

## Пруфы
- pytest 57/57 + backend 6/6 + harness на хосте; soak 60с без панели:
  CONNECTED до/после, рестартов 0; triple-pass; v0.1.6 в релизах.
