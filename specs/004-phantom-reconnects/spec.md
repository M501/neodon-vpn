# Spec 004 — мнимые реконекты (flow-track)

## Жалоба
Таймер сбрасывается ~раз в час; v2RayTun держит 24ч. Подозрение на рестарты.

## Диагноз (живой хост, факты)
- `sing-box.service`: ActiveEnterTimestamp = мой тестовый свитч, NRestarts=0,
  watchdog ok/0 failures. Вотчдог-таймера и скрипта вообще нет в системе.
- Вывод: сервис НЕ падает и НЕ рестартится. Сбросы — GUI: `_off_streak>=3`
  чистил эпоху после 3 подряд не-CONNECTED опросов на просадках egress.

## Фикс (GUI, один путь)
- Гистерезис состояния: CONNECTED держится при 1–2 подряд деградациях
  (CONNECTING/DEGRADED/STARTING/TRANSITIONING); 3-я принимает. OFF/FAILED/
  LOCKED — всегда сразу; смена desired — сразу (свитчи не тормозят).
- Счёт streak унифицирован (эпоха и пилюля считают одно и то же).
- `transitions.log` в STATE_DIR (кап 20Кб/150 строк): время, переход,
  desired, exit — различие рестарта и мигания доказывается файлом.

## Verify
21/21 pytest хост (4 новых) → деплой → GUI рестарт → CHANGES + push.
Следующий «сброс»: смотреть transitions.log (будет видно, было ли OFF/рестарт).
