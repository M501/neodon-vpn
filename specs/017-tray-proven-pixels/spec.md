# Spec 017 — трей доказан пикселями + строки не рвутся (flow-track)

## Обвинение (справедливое)
«Пиздёж, нихуя не проверил»: трей показывал шилд, строки серверов рвались.

## Корни
1. Трей: `_status_loaded` НИКОГДА не звал `_sync_tray` — опросы иконку не
   обновляли в принципе (древний баг, мой флаг-код был мёртв с рождения).
   Фикс: вызов в воронке + зеркало `tray-state.json` (диагностика навсегда).
2. Строки: rebuild сетки при пинге убран ранее; остатки PL/NO/SE рвались
   длинными именами → truncation 24 символа на уровне данных + тест.
3. Retry exit-пробы против пачек DEGRADED (4×/7мин при ротации egress).

## Живые пруфы (не слова)
- tray-state.json живого процесса: `CONNECTED · PROXY · NEODON, icon=flag`.
- Спектракл fullscreen + кроп трея: польский флаг на месте, шилда нет.
- Скрин окна: PROXY, CONNECTED, квота, флаги серверов — всё рендерится.
- 41/41 pytest; деплой + рестарт.

## Открыто
- Папка с фото v2RayTun-UI не найдена (Screenpresso/Screenshots — мимо);
  нужен путь от владельца для спека менеджера подписок.
- Менеджер подписок (clipboard/file/QR/delete/export) — спек 018 следующим.
