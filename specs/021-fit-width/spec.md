# Spec 021 — ширина: влезает всегда (flow-track)

## Жалоба
Окно уже контента: тянуть вправо; тач таскает UI влево-вправо.

## Корень
Minimum меток = полный текст → сетка шире вьюпорта → h-диапазон →
тач-скроллер его катал. Лимит в символах слеп к DPI.

## Фикс
- `_ElidedLabel.minimumSizeHint = 0` (elide рисует … по пикселям) —
  h-диапазона больше нет физически, катать нечего.
- Геометрия: save в closeEvent, restore на старте, merge в write_gui_state
  (иначе опросы тёрли geom).
- Живой пруф: дефолтная ширина, `53669…`/`TikT…`, колонки целы.

## Verify
47/47 pytest (minsize + roundtrip + merge) → деплой → рестарт → vision.
