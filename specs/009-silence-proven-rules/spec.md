# Spec 009 — тишина уведомлений + таб правит реальностью (flow-track)

## Жалобы
1. NotifySend-спам при каждом изменении — вырубить всё.
2. Таб Traffic rules выглядит неизменно; недоверие: «юзаются ли эти
   правила вообще или другие».

## Фикс уведомлений (в корне)
- `notify()` в toggle.sh → no-op; инлайн-пинг в server.sh → `:`; Popen
  «Подключено» из GUI удалён (тест перевёрнут: Popen обязан молчать).
- Найден и обезврежен self-bug: `#`-коммент в замене съедал `; fi`
  (syntax error) — только голый `:` внутри строк с хвостом.
- Оставлен разовый трей-хинт при сворачивании (не спам изменений).
- Проверка: 0 notify-send в обоих скриптах (хост), 27/27 pytest.

## A/B доказательство (таб правит реальностью)
- ru-bez-vpn: exit .112 (VPN), youtube→proxy. popular-ai: exit .190 (ISP!),
  youtube→direct. Обратно ru-bez-vpn: exit .101, youtube→proxy.
- Один YouTube — три разных пути по клику на карточку. Вопрос закрыт цифрами.

## Честный таб
- Бейджи: «✓ проверено вживую» / «○ ещё не проверялось» (vision-пруф рендера).

## Verify
Деплой + рестарт → CHANGES + push.
