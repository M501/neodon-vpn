# Spec 012 — прямые ответы: 3 кастома, галочка, флаг, verified (flow-track)

## Вопросы владельца (дословно по смыслу)
1. Три нестандартных пресета, которых нет в v2RayTun — что это.
2. Зелёная галочка только у одного — почему.
3. Флаг .RU пропал (я удалил уродца) — вернуть.
4. Что verified значат; подозрение: UI скопирован, rules под капотом нет.

## Ответы делом
1. Кастомы: `default` (наш базовый fallback, включается при выкл. Community
   rules), `ai`/`anti-censorship` (V1-наследие прошлой нейронки, файлы на
   хосте, работают). Work из эталона у нас нет. Удалять без спроса не стал.
2. Галочка = маркер АКТИВНОГО (не качества). Теперь текстом «● АКТИВЕН»
   на карточке + в диалоге (тесты).
3. Флаг возвращён (перекачан) + crop-fill: чёрные поля обрезаны, 28px
   проверен глазами — флаг во всю ширину.
4. verified переопределён честно: ✓ = live end-to-end (ru-bez-vpn: матрица+
   живой трафик+A/B; popular-ai: A/B). Остальным, включая default и
   social-networks, сброшен в «ещё не проверялось» — заявлений без пруфов
   больше нет.
5. Механизм (не картинки): пресет GUI → apply-profile.py → route.rules
   живого config.json → поведение sing-box. Цепочка показана живьём:
   `.profile=ru-bez-vpn`, avito в конфиге, `avito.ru → direct`.

## Verify
35/35 pytest (3-tuple обеих сессий сведён) → vision (иконка 28px, таб) →
деплой → рестарт → CHANGES + push.
