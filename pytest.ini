[pytest]
addopts = -ra
pythonpath = .
testpaths = tests
markers =
    l1: fast/offscreen/unit/contract checks
    l2: isolated component checks
    live: live host tests (not run unless explicitly enabled)
